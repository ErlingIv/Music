// Site-wide configuration
// Edit SITE_NAME to change the title shown in the header and browser tab.
const SITE_NAME = 'Music Scores';

// Single source of truth for the Supabase project's REST endpoint and
// publishable (anon) key — previously duplicated as a literal string in
// every page's own inline script, so a project migration or key rotation
// meant updating each one by hand. Load this before any page script that
// needs them.
const SUPABASE_URL = 'https://tfqnzszyjsdgdeksizel.supabase.co';
const SUPABASE_KEY = 'sb_publishable_TxNG1PKrOD3NuBwCKzEfMA_b3-21kij';
